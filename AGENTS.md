# AGENTS.md

## 根本目标

本项目的根本目标是：以冻结的原始 PDF 为最终内容与视觉事实源，以与其精确绑定的 MinerU + Popo 输出为正式处理基础，完整、准确、自动、忠实、稳定地重建教育材料，并套入固定 ElegantBook 模板，生成可独立编译、复现、审阅和交付的 LaTeX ZIP/PDF。

必须保持正文、阅读顺序、目录结构、公式、表格、图片及教学语义完整；对来源中有明确依据且模板正式支持的教学内容，应正确、一致、充分但不过度地使用 `tcolorbox` 等模板构件。

自动化目标是从冻结输入贯通到等待用户验收。真实歧义必须进入 `needs_review`，不得猜测、编造、降低门禁或制造假成功。用户验收始终是独立状态。

## 主线方向

项目必须始终围绕以下主线推进：

```text
冻结 PDF + MinerU + Popo
→ 确定正文范围和阅读顺序
→ 重建目录与层级
→ 按目录分块、清洗和整理正文
→ 识别教学语义并准确映射到固定 ElegantBook 模板
→ 生成、编译和审计 LaTeX
→ 对照原始 PDF 持续比对、修订和优化
→ 输出正式 ZIP/PDF，等待用户验收
```

具体阶段、模块、模型、工具和内部实现可以在开发中持续拆分、合并或调整，不固定阶段数量；但不得偏离主线、破坏证据链或以局部实现替代最终目标。

主线尚未贯通时，优先解决最早阻断点和通用问题。不得长期纠缠不影响内容正确性、模板一致性和正式交付的局部细节。

## 事实与边界

- 原始 PDF 是最终内容和视觉事实源。
- MinerU + Popo 是正式转换入口和结构化基础证据，但不能覆盖原始 PDF。
- 发生冲突、缺失或歧义时，必须回到 PDF 核验并保留证据，不得静默选择方便下游处理的结果。
- 上游内容、目录或语义缺陷必须在最早责任位置解决，禁止用下游排版补丁掩盖。
- 已冻结且仍然有效的上游产物不得因下游失败而重复执行。
- 原始输入、阶段候选、独立评估、正式输出和用户验收属于不同证据层，不得混为一个“完成”状态。

## 开发原则

- 先核查当前代码、运行状态、真实产物、测试和既有修改，再作判断或实施。
- 用户提出的根因、方案和完成判断属于待验证假设；明确命令决定授权范围，但不能改变事实、质量标准和验收结论。
- 紧扣根本目标主动判断，不顺从性接受观点，也不为了表现独立而机械反对。
- 默认采用最小而完整的修订，从最早根因处解决问题，避免推倒重来和无边界重构。
- 如果局部修补已经无法满足根本目标，必须先用证据说明原因，并提出有范围、兼容、迁移、回滚和验收方案的受控调整。
- 禁止按书名、文件名、材料 ID、固定页码、固定文本或特定样本编写生产逻辑。
- 修复必须基于通用结构、语义、来源证据和正式契约，并通过多种教育材料回归验证。
- 不得为了让已知样本或报告变绿而过拟合、降低门禁或增加隐性 fallback。
- 不修改无关代码，不覆盖其他未提交工作，不破坏历史产物和运行数据。

## 验收底线

任何完成结论都必须回到端到端产品结果，至少验证：

- 正文范围、阅读顺序及内容媒体守恒；
- 目录、分块和教学语义准确；
- `tcolorbox` 等模板构件覆盖充分、类型正确且无滥用；
- 固定 ElegantBook 模板零漂移；
- 无编译错误、缺字、缺图和未授权替代；
- 最终 ZIP 可在独立环境复编；
- 最终 PDF 全页对照原始 PDF 审阅；
- 用户可见功能通过真实页面验证；
- `needs_review`、机器完成、等待验收和用户接受保持独立。

单个样本通过、测试通过、编译成功或生成 ZIP/PDF，都不能单独证明项目完成。成功率必须说明样本分母、材料类型、失败及人工审阅分布。

## 产品总监线程

产品总监线程负责核查事实、维护目标和边界、判断优先级、制定任务书及审查开发证据，默认不直接修改代码或生产数据。

提出新任务前，应先检查当前进展，向用户说明目标、方案、取舍和验收标准并等待确认。开发成果必须按根本目标独立审查，从最早失败位置决定接受、修订或驳回。

## 安全与验证

- 未经授权，不修改冻结输入、删除数据、提交、推送或部署。
- 不输出或提交密钥、密码及私密配置。
- 后端：`cd backend && uv run pytest tests -q`
- 前端：`cd frontend && npm run build`
- 按修改范围补充聚焦测试、回归样本、Compose 校验、独立复编和浏览器 UAT。
- 修改代码后运行 `./graphify update .`。
- 具体实现、阶段契约和发布规则以 `docs/` 中当前批准的版本化文档为准；它们可以演进，但不得违背本文件的根本目标和边界。
