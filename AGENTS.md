# AGENTS.md

## 项目约定

- 修改前先阅读相关代码、测试和规范，明确成功标准。
- 采用最小、通用的修改，禁止样本专用硬编码。
- 不修改无关代码，不覆盖已有未提交工作。
- 修复缺陷时先复现并增加回归测试。
- 用户可见功能变更必须通过真实页面验证，API 检查不能代替页面验收。
- 不降低质量门禁，不把局部成功描述为整体完成。
- 未经授权，不删除生产数据、修改冻结产物、提交、推送或部署。
- 不输出或提交密钥、密码及私密配置。

## 验证

- 后端：`cd backend && uv run pytest tests -q`
- 前端：`cd frontend && npm run build`
- 根据修改范围补充聚焦测试、Compose 校验和浏览器 UAT。
- 修改代码后运行 `./graphify update .`。

## 专项规范

具体流程、架构、验收标准和发布规则以 `docs/` 中对应文档为准。
